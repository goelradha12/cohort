name: Deploy Backend

on:
  push:
    branches:
      - main

env:
  AZURE_WEBAPP_NAME: cook-the-code-api
  NODE_VERSION: '24.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: MyLeetlab/backend/package-lock.json

      - name: Install dependencies
        working-directory: MyLeetlab/backend
        run: npm ci

      - name: Generate Prisma Client
        working-directory: MyLeetlab/backend
        run: npx prisma generate

      - name: Build
        working-directory: MyLeetlab/backend
        run: npm run build

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy to Azure
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          package: MyLeetlab/backend